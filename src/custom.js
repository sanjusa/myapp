window.onload = Array.prototype.myfunc = function(){

    return []
}
window.addEventListener('load', event =>{
   //alerts();
 
})

function customFunc(){
   return Array.prototype.masking = function(){
        for( let i=0;i<this.length;i++)
        {
        this[i] = this[i].split('').fill('#',0,3).join('');
    
        }
    }
}


